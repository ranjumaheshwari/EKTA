import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFFDFCFB); // Warm white
  static const Color foreground = Color(0xFF3A2D25); // Dark brown text

  // Saffron (Primary)
  static const Color primary = Color(0xFFFF9933);
  static const Color primaryLight = Color(0xFFFFB870);

  // Green (Success/Secondary)
  static const Color success = Color(0xFF138808);
  static const Color successLight = Color(0xFF90EE90);

  // Blue (Trust/Accent)
  static const Color trust = Color(0xFF000080);
  static const Color accent = Color(0xFF265C8C);

  static const Color card = Color(0xFFFFFBF8);
  static const Color border = Color(0xFFE5D5C5);

  static const Color muted = Color(0xFFF2E8DF);
  static const Color mutedForeground = Color(0xFF8C7B70);
}